						{{#each text}}
							<div class="content-block-title" style="text-align:center;">{{titulo}}</div>

							<div class="content-block" style="text-align:center;">
								{{#if imagem}}
									<img class="img_medium" onerror="imgError(this);" src="{{imagem}}?time={{modificado}}">
								{{else}}
									<img class="img_medium" src="img/dummy480.png">
								{{/if}}
							</div>
							<div class="content-block" style="margin:10px auto; text-align:center;">
								<a href="#" class="button btn_foto_perfil">Alterar minha foto</a>
							</div>
						{{/each}}






