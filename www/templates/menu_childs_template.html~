					<div class="menu_childs">
						<h3>Escolha um filho</h3>
						{{#each text}}
						<div class="menu_child_box">
							{{#if imagem}}
								<img id="{{id}}" class="img_menu_child" src="{{imagem}}" />
							{{else}}
								<img id="{{id}}" class="img_menu_child" src="img/dummy48.png" />
							{{/if}}
							</br>
							{{nome_aluno}}
						</div>
						{{/each}}
					</div>

